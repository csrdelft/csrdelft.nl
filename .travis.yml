language: node_js
node_js:
  - "8"

addons:
  apt:
    packages:
      - libpng-dev

cache:
  directories:
    - "node_modules"

install: npm install

script:
  - npm run production

deploy:
  provider: script
  script: ./bin/ci/deploy.sh
  skip_cleanup: true
  on:
    branch: master